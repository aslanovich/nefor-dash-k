// / — Главная. Вариант v1/v2 переключается тогглом в панели tools (без смены роута).
import MainDashboard from "@/components/dashboard/MainDashboard";

export default function Home() {
  return <MainDashboard />;
}
